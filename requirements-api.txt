# Production runtime dependencies for the FastAPI inference service.
# Keep this file minimal: only packages imported by api/ and the runtime src/ modules.
fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.13.4
pandas==2.3.3
numpy==2.4.6
scikit-learn==1.9.0
prometheus-client==0.25.0
prometheus-fastapi-instrumentator==8.0.2
joblib==1.5.3
