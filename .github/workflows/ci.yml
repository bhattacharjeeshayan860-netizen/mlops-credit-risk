name: CI/CD #Name of the workflow

on: # Triggers on push or pull request events for the master branch
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

jobs:
  test: # This job runs on Ubuntu
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-api.txt
      - name: Run tests
        run: python -m pytest
      - name: Login to DockerHub
        if: github.event_name=='push'
        uses: docker/login-action@v3.0.0
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME}}
          password: ${{ secrets.DOCKERHUB_TOKEN}}
      - name: Build Docker image
        run: |
          docker build \
            -t shayanbhattacharjee/credit-risk-api:latest \
            -t shayanbhattacharjee/credit-risk-api:${{ github.sha }} \
            -f docker/Dockerfile .
      - name: Push Docker image to DockerHub
        if: github.event_name == 'push'
        run: |
          docker push shayanbhattacharjee/credit-risk-api:latest
          docker push shayanbhattacharjee/credit-risk-api:${{ github.sha }}
